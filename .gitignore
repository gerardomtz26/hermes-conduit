# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
Package.resolved

# Local Git worktrees
.worktrees/

# macOS
.DS_Store
*.swp
*~

# Secrets
.env
*.p8
*.p12
*.mobileprovision
TestResults*.xcresult
ci-logs/

# CI local run directories (the CI workspace uses the same paths)
ci-derived-data/
ci-lane/
ci-artifacts/
.scratch-obs/
__pycache__/

# Agent-session artifacts (stray SDD reports previously leaked in via a PR)
.superpowers/

# Local agent workspace (machine-specific, not for the public repo)
AGENTS.md
.ios-test.json
.dsh/
.zcode/
workflows/
.test-runs/